# AI 电商产线工位地图（前台入口）

> 本文件是「AI 电商产线」的唯一入口。**用户不需要在几百张卡里挑卡**——按下面的工位走，
> 每个工位一个单文件技能，工位之间靠《产线事实表》交接。
>
> 工位号 E0–E7 与证据等级 E1–E5 同名不同义，凡表述带前缀（「工位 E1」/「证据 E1」）。
> 当前状态：工位 E1 主图 / E4 场景植入 / E5 平面海报 / E7 详情长图 = 证据 E1（2026-09-23 安途代步车真实案例 + 当前模型 + 真实交付物验证）；
> E2 / E3 / E6 / E0 = 证据 E4（成熟方法重铸，未经当前模型实测）。
> 剩余的 E2 / E3 / E6 三张卡仍待实战：跑通后请把实测结论回填到对应工位的 R 段与 A1 段，并把 evidence 提升为 E1。
>
> 短剧产线已公开（S1–S6，入口见《双产线工位地图》），本文只覆盖电商线。

---

## 一、总原则

1. **先预览、确认、再掏钱。** 任何调用付费生成接口之前，先把本批的内容、数量、参数、参考、输出摊开给用户看，取得明确确认再执行。这条是硬纪律，写进了每个工位。
2. **真实画面比形容词可靠。** 凡是必须保真的东西（商品形状、logo、手持物），给它一张真图做锚点，不要试图用提示词描述住。
3. **不可变短语跨图逐字复用。** 商品造型、颜色、logo 各写一条短语，改一个字等于开新 SKU。
4. **一次只换一层参数。** 环境、背景、影调是可分别替换的三层；同时改会互相打架。
5. **平台参数不写死。** 时长上限、尺寸、白底占比等，一律进《产线事实表》现填现用，以目标平台现行文档为准。
6. **模型按任务挑。** 平面感/排版任务选擅长排版的模型；光影氛围任务选擅长光的模型。选错模型，返工两次抵掉全部收益。

---

## 二、AI 电商产线（7 工位）

```
一张原始商品照
        │
        ▼
 [E1] 电商主图 ───────────► 干净商品图 + 打光 + 合规主图/副图
        │
        ├─► [E2] SKU 变体矩阵 ──► 配色/材质/款式变体登记表
        │
        ├─► [E3] 产品三视图 ────► 多视角图 + 十六宫格粗筛
        │
        ├─► [E4] 场景植入与氛围 ► 使用场景图 + 氛围参考图换影
        │
        ├─► [E5] 平面海报排版 ──► 海报/详情图/社媒图（含文案位）
        │
        ├─► [E6] 产品运镜视频 ─► 环绕展示/迭代变形/节奏走秀
        │
        └─► [E7] 详情页长图 ───► 竞品拆解 → 模块规划 → 逐模块带字生成 → 750 宽拼接
```

| 工位 | 目录 | 什么时候用 |
|---|---|---|
| E1 | `jd-ec-main-image` | 只有手机随手拍，要上架主图 |
| E2 | `jd-ec-variant-matrix` | 要多个配色/材质/款式 |
| E3 | `jd-ec-triview` | 要多角度图、三视图 |
| E4 | `jd-ec-scene-infuse` | 商品要进场景、氛围要高级 |
| E5 | `jd-ec-key-visual` | 要海报、详情图、社媒图 |
| E6 | `jd-ec-product-video` | 静态图要变成产品短片 |
| E7 | `jd-ec-detail-page` | 商品资料要做成一条完整详情长图（含竞品拆解） |

不知道从哪个工位开始？激活门房「`选包门房`」（`scene-recipe-concierge`），三问定位。

---

## 三、《产线事实表》（每个项目填一次，其余工位自动引用）

```markdown
## 项目
- 项目名：
- 产线：电商
- SKU 编号：

## 平台与硬约束（以平台现行文档为准）
- 目标平台：
- 主图纯白底：是/否
- 商品占画比要求：待复核
- 是否允许叠加文字：
- 画幅选项与默认画幅：
- 单段视频时长上限：
- 是否支持参考引用（行内 @ / 编号指代）：
- 是否支持负向词：

## 资产
- 商品不可变短语：
- 成像基底（主机身型号）：
- 已冻结的场景图编号：

## 待用户确认项
- （逐条列出假设值，未经确认不得进入付费生成）
```

---

## 四、合规与来源说明

- 7 个工位的方法论来源有三：① 公开影视/影像工业通用方法（运镜、打光、剪辑等公共知识）；
  ② 内部实战笔记重铸；③ 对 MIT 开源项目 `motiful/product-shots` 的**结构借鉴**——
  只借鉴产线分层结构，未复制任何第三方技能原文提示词，MIT 署名义务由每个工位 frontmatter 的
  `acknowledgment` 字段逐条履行（见根目录 `THIRD-PARTY-LICENSES.md`）。
- 证据 E4 的工位不声称已实测验证；平台硬约束数值一律标注「以目标平台现行规则为准」。

---

## 五、下一步（待确认）

1. E2 / E3 / E6 三张卡跑完首次实战后，把证据等级从 E4 回填为 E1，并把实测结论写进 R 段与 A1 段。
2. 上游种子投稿（E0–E7 + 门房）PR 合并后，本仓库预设即可以「已上架」状态分发。
3. 电商产线起步预设已占满 8 个激活位（E1–E7 + E0）；后续若再增工位，需拆分为多个预设。
