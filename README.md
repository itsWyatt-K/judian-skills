# judian-skills（剧典技能库 · 公开版）

> **v3.1.0 · 2026-09-23 · 双产线工位版**
>
> 公开版 = **AI 短剧产线 6 工位 + AI 电商产线 6 工位 + 画质净化公共附录（E0）+ 门房**。
> 从「34 个域包 / 759 张卡 + 56 个单文件技能」收缩为「两条产线、十三个工位」——
> **为什么缩容 = 聚焦**：选择本身是负担——用户不该在几百张卡里挑卡，Agent 也不该在过大的检索空间里猜。
> 收缩后检索空间小而准、场景路由确定性高，**更有利于聚焦生成**。
> 历史域包已从公开版撤下（本地全量库继续维护，见文末「版本轨迹」）。

## 三十秒上手（新手友好路径）

1. **装门房**：种子市场搜「`选包门房`」并激活。不知道从哪开始时，它会用三个问题把你送到对的工位。
2. **装产线预设**：搜「短剧产线起步」或「电商产线起步」一键挂齐整条线；或逐个搜工位中文名安装。
3. **按工位走**：打开《双产线工位地图》→ 填一次《产线事实表》→ 从该产线的第一个工位开始，一个工位一个交付物。
4. **纪律只有一条**：每个工位在调用付费生成前，都会先把本批内容 / 数量 / 参数 / 参考摊开给你看，**取得确认才执行**。

## 这个仓库是什么

一批**影像创作产线的方法论工位技能**，装在影策（Open AI Canvas）里给 Agent 用。
每个工位 = 一个单文件技能（SKILL.md），工位之间靠一份《产线事实表》交接；门房负责指路。

它不生产画面，也不替代影策的生成链路；它负责**生成之前的判断与工序**：
短剧线管结构怎么搭、镜头怎么排、衔接怎么接；电商线管怎么拍、怎么打光、怎么保真、怎么合规。

## 工位一览（中文真名在前，括号内为目录名）

### 短剧产线（S1–S6）

| 工位 | 技能名（目录名） | 什么时候用 |
|---|---|---|
| S1 | `短剧分镜表`（`jd-drama-shot-list`） | 有文字素材，要排镜头 |
| S2 | `角色资产板`（`jd-drama-asset-board`） | 要角色设定图、三视图、角色档案 |
| S3 | `影视提示词编译`（`jd-drama-prompt-compile`） | 要把意图写成提示词 |
| S4 | `首尾帧与镜头衔接`（`jd-drama-frame-bridge`） | 两段要接、要转场、要循环 |
| S5 | `跨镜一致性锁定`（`jd-drama-consistency`） | 换人、换背景、氛围不对、接着演 |
| S6 | `成片装配`（`jd-drama-assembly`） | 素材齐了，要剪成片并交付 |

### 电商产线（E0–E6）

| 工位 | 技能名（目录名） | 什么时候用 |
|---|---|---|
| E1 | `电商主图`（`jd-ec-main-image`） | 只有手机随手拍，要上架主图/副图 |
| E2 | `SKU变体矩阵`（`jd-ec-variant-matrix`） | 要多个配色/材质/款式变体 |
| E3 | `产品三视图`（`jd-ec-triview`） | 要多角度图、三视图 |
| E4 | `场景植入与氛围`（`jd-ec-scene-infuse`） | 商品要进场景、氛围要高级 |
| E5 | `平面海报排版`（`jd-ec-key-visual`） | 要海报、详情图、社媒图 |
| E6 | `产品运镜视频`（`jd-ec-product-video`） | 静态图要变成产品短片 |
| E0 | `画质净化公共卡`（`jd-ec-quality-scrub`） | 画面油腻/塑料感/AI 味/噪点（公共附录） |
| 门房 | `选包门房`（`scene-recipe-concierge`） | 不知道选哪个、模糊需求先问路 |

> **检索提示**：技能商店 / 种子市场搜**中文名**（如「电商主图」「短剧分镜表」）即装；
> 英文目录名仅用于 GitHub 安装路径，不要当检索词用。

## 证据等级（如实说明）

- 全部工位为 **E4**：专业上成立，**未经本团队当前模型实测验证**。电商线已跑首次实战（实测结论回填中）；首次实战跑通后回填实测结论。
- 没有「装上就提升成片质量」的承诺。v2.0.0 时代的 A/B 记录留档在 `benchmarks/`，边界说明见各报告原文。

## 文档

- [`docs/双产线工位地图.md`](docs/双产线工位地图.md) —— **唯一入口**：总原则 + 双线流程 + 《产线事实表》模板
- [`docs/电商产线工位地图.md`](docs/电商产线工位地图.md) —— 电商单线版（只有电商需求时看这份）
- [`docs/scene-starter-picks.md`](docs/scene-starter-picks.md) —— 新手场景推荐表：说人话 → 装哪个工位
- [`presets.json`](presets.json) —— 场景预设（平台层分发用）；`presets_check.py` 为对账门禁，`seed-snapshot.json` 为种子快照
- [`THIRD-PARTY-LICENSES.md`](THIRD-PARTY-LICENSES.md) —— 第三方许可与署名（两个 MIT 项目的结构借鉴）
- [`seed-contribution/`](seed-contribution/) —— 上游种子投稿素材；`id_ledger.json` 为 ID 台账（编号一经分配永不变更）
- [`benchmarks/`](benchmarks/) —— v2.0.0 时代 A/B 实测记录（历史存档）

## 许可

- **judian 自撰内容**：MIT（见 [LICENSE](LICENSE)）；
- **第三方结构借鉴**：两个 MIT 项目（详见 [THIRD-PARTY-LICENSES.md](THIRD-PARTY-LICENSES.md)）——
  只借鉴产线分层结构，未复制任何第三方提示词原文；署名义务由每个工位 frontmatter 的
  `acknowledgment` 字段逐条履行。

## 与上游的协作

- 已合并（v2.0.0 时代）：#568 卡级种子 / #575 种子市场重组为 68 条 / #577 description 注入 Agent manifest
- v3.1.0（已提交）：上游单 PR **#611**（ddcat-ai/open-ai-canvas）——种子 68→47（删 34 域包 + E0–E6 + S1–S6 + 门房更新）+ Agent 双产线引导策略 v9 + 短剧场景预路由；等待上游审核
- 说明：上游仓库 CI 当前在 main 上即为双红（Web / Backend checks 均 failure，与本文 PR 内容无关），#611 本身 mergeable=True；Web 检查失败点为仓库既有测试 `web/test/generation-storage-consistency.test.ts` 的相对 URL 解析 bug

## 版本轨迹

- **v3.1.0（2026-09-23）**：双产线工位版。公开版 = S1–S6 + E0–E6 + 门房。
- **v3.0.0（2026-09-23）**：聚焦电商单产线（E1–E6 + 门房）；其聚焦逻辑被 v3.1.0 继承（域包不再回归）。
- **v2.0.0（2026-09-22）**：34 域包 / 759 卡 + 56 singles + 门房。**已从公开版撤下**；
  本地全量库（`_backup/20260923-judian-skills全量`）完整保留；`benchmarks/` 为其时代 A/B 实测档案（有意保留）。
